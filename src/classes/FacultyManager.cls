public with sharing class FacultyManager {

	public static void getFacultyListForParents(Id ParentAccId){
		set<Id> StudentsDivisionIds = StudentManager.getDivisionsSet(ParentAccId);
		//set<Id> TimeTableIds = 		
	}

}